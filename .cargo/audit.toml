# cargo-audit configuration for the SAKURA-II workspace.
# Advisories listed under [advisories] ignore are tracked in
# docs/standards/deviations.md — every entry requires a D-### deviation record.

[advisories]
ignore = [
    # RUSTSEC-2024-0436: `paste` crate — no longer maintained.
    # Transitive path: cfdp-rs 0.3.0 → spacepackets 0.16.1 → paste 1.0.15.
    # We do not have a direct dependency on `paste`; the cfdp-rs crate author
    # has not yet published a release that replaces it.  Tracked in D-003.
    # Sunset: remove once cfdp-rs ships a release without the paste dependency.
    "RUSTSEC-2024-0436",
]
