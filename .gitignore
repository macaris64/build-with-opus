# ─── Claude Code ─────────────────────────────────────────────────────────────
# Personal overrides — never commit
CLAUDE.local.md
.claude/settings.local.json
.claude/agent-memory-local/
.claude/plans/

# ─── Rust ─────────────────────────────────────────────────────────────────────
# Cargo.lock is committed in this workspace (it has binary members).
# For library-only crates, Cargo.lock is conventionally gitignored.
target/
.cargo/registry/
.cargo/git/

# Nested-workspace fixture (Phase 01 lint probe) — its own Cargo.lock is
# ephemeral; the probe is only built to verify it FAILS under workspace lints.
rust/_lint_probe/Cargo.lock

# ─── C / CMake ────────────────────────────────────────────────────────────────
build/
build_cov/
# cFS cmake build outputs — only the stub binary is tracked
build_cfs/apps/
build_cfs/cfe_build_env.in
build_cfs/cfe_module_version.in
build_cfs/cfs/
build_cfs/cmake_install.cmake
build_cfs/CTestTestfile.cmake
build_cfs/DartConfiguration.tcl
build_cfs/docs/
build_cfs/inc/
build_cfs/Makefile
build_cfs/mission_vars.cache
build_cfs/native/
build_cfs/simulation/
build_cfs/src/
build_cfs/tables/
*.o
*.a
*.d
*.gcda
*.gcno
*.gcov
*.deb
# Temp scaffold used to smoke-test the Phase 19 template mechanism
apps/hello_app/
compile_commands.json
CMakeCache.txt
CMakeFiles/

# ─── ROS 2 / colcon ───────────────────────────────────────────────────────────
ros2_ws/install/
ros2_ws/build/
ros2_ws/log/
log/

# ─── Secrets & environment ────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
!*.key.example

# ─── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
*.so

# ─── macOS ────────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ─── Editors ──────────────────────────────────────────────────────────────────
.idea/
.vscode/settings.json
.vscode/launch.json
*.swp
*.swo
*~

# ─── OS ───────────────────────────────────────────────────────────────────────
Thumbs.db
ehthumbs.db

# ─── Logs ─────────────────────────────────────────────────────────────────────
*.log
logs/
